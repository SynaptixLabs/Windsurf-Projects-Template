# {{PROJECT_NAME}} — Production Environment
# ALL values must come from CI/CD vault or secret manager.
# This file documents the REQUIRED keys only — never store values here.

APP_ENV=production
DEBUG=false
LOG_LEVEL=warning

SECRET_KEY=REQUIRED_FROM_VAULT
DATABASE_URL=REQUIRED_FROM_VAULT
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=40

PORT=8080
PUBLIC_URL=REQUIRED_FROM_VAULT

ALLOWED_ORIGINS=REQUIRED_FROM_VAULT

ANTHROPIC_API_KEY=REQUIRED_FROM_VAULT
LLM_MODEL=claude-sonnet-4-6

STORAGE_BACKEND=s3
AWS_ACCESS_KEY_ID=REQUIRED_FROM_VAULT
AWS_SECRET_ACCESS_KEY=REQUIRED_FROM_VAULT
AWS_S3_BUCKET={{PROJECT_NAME}}-production
AWS_REGION=REQUIRED_FROM_VAULT

SENTRY_DSN=REQUIRED_FROM_VAULT
SENTRY_ENVIRONMENT=production

# Feature flags (prod: all experimental flags OFF by default)
# FEATURE_FLAG_X=false
