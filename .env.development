# {{PROJECT_NAME}} — Development Environment
# Safe defaults for local development. Commit to git? NO.
# Copy .env.example → .env.development, then fill in your local values.

APP_ENV=development
DEBUG=true
LOG_LEVEL=debug

SECRET_KEY=dev-secret-not-for-production-change-me

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/{{db_name}}_dev
# DATABASE_URL=sqlite:///./{{db_name}}.db

PORT={{DEV_PORT}}
HOST=localhost
PUBLIC_URL=http://localhost:{{DEV_PORT}}

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:{{DEV_PORT}}

# Uncomment and fill when using AI features locally
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# LLM_MODEL=claude-haiku-4-5-20251001

# Local storage
STORAGE_BACKEND=local
STORAGE_LOCAL_PATH=./data/uploads

# Feature flags (dev: enable experimental features freely)
# FEATURE_FLAG_X=true
