# {{ project_name }}

> {{ project_description }}

This project was generated with the [Windsurf Python Modern Template](https://github.com/Synaptix-Labs/Windsurf-Projects-Template).

## 🚀 Quick Start

1.  **Install dependencies:**
    ```bash
    poetry install
    ```

2.  **Activate pre-commit hooks:**
    ```bash
    poetry run pre-commit install
    ```

3.  **Run the tests:**
    ```bash
    poetry run pytest
    ```

4.  **Run the application:**
{% if integration_focus == 'web_api' %}
    ```bash
    poetry run uvicorn {{ project_slug }}.main:app --reload
    ```
{% elif integration_focus == 'ai_first' %}
    ```bash
    poetry run python src/{{ project_slug }}/app.py
    ```
{% elif integration_focus == 'data_processing' %}
    ```bash
    poetry run python src/{{ project_slug }}/pipeline.py
    ```
{% elif integration_focus == 'full_stack' %}
    ```bash
    poetry run uvicorn {{ project_slug }}.main:app --reload
    ```
{% else %}
    *(Add your run command here)*
{% endif %}

5.  **Start developing!**
    - Your main application code is in `src/{{ project_slug }}/`.

## 🛠️ Development

This project uses [Poetry](https://python-poetry.org/) for dependency management and [Ruff](https://beta.ruff.rs/) for linting and formatting.

-   **Run tests:** `poetry run pytest`
-   **Check for linting errors:** `poetry run ruff check .`
-   **Format code:** `poetry run ruff format .`

## 📝 License

This project is licensed under the MIT License. See the `LICENSE` file for details.
