"""Instructor Data Analysis Example"""
import os
from dotenv import load_dotenv
from rich.console import Console
from src.{{project_slug}}.instructor_utils import extract_users

load_dotenv()

console = Console()

if __name__ == "__main__":
    if not os.getenv("OPENAI_API_KEY"):
        console.print("[bold red]Error: OPENAI_API_KEY is not set.[/bold red]")
    else:
        text = "John (age 25) and Mary (age 30) are colleagues."
        users = extract_users(text)
        console.print(users)
